@page
@model Kiki.WebApp.Pages.Users.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.Utilisateur[0].Email)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Utilisateur[0].UserName)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Utilisateur[0].Password)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Utilisateur[0].Entreprise)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Utilisateur[0].Nom)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Utilisateur[0].Prenom)
                </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Utilisateur) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Email)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.UserName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Password)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Entreprise)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Nom)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Prenom)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.Id">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
